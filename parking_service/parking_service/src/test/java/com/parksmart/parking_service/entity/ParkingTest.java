/*package com.parksmart.parking_service.entity;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class ParkingTest {

    @Test
    void testParkingEntity() {

        Parking parking = new Parking();

        parking.setId(1L);
        parking.setLocation("MG Road");
        parking.setTotalSlots(100);
        parking.setAvailableSlots(50);
        parking.setPricePerHour(30.0);

        assertEquals(1L, parking.getId());
        assertEquals("MG Road", parking.getLocation());
        assertEquals(100, parking.getTotalSlots());
        assertEquals(50, parking.getAvailableSlots());
        assertEquals(30.0, parking.getPricePerHour());
    }
}*/